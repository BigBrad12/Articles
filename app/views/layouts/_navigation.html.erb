<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand", id:"logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li><%= link_to "Create Article", new_article_path, class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to "View Articles", articles_path, class: "dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
          </ul>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Categories
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? && current_user.admin? %>
              <li><%= link_to "Create Category", new_category_path, class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to "View Categories", categories_path, class: "dropdown-item" %></li>
          </ul>
        </li>


      <% if logged_in? %>
        <li class="nav-item dropdown">

          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= "(Admin)" if current_user.admin? %> [ <%=  current_user.username  %> ]
          </a>

          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li><%= button_to "View Profile", user_path(current_user), method: :get, class: "dropdown-item" %></li>
              <li><%= button_to "Edit Profile", edit_user_path(current_user), method: :get, class: "dropdown-item" %></li>
              <li><%= button_to "Delete Profile", user_path(current_user), method: :delete, class: "dropdown-item text-danger" %></li>
            <% end %>
          </ul>

        </li>
      <% end %>

        <li class="nav-item">
          <%= button_to "Authors", users_path, method: :get, class: "nav-link active", id: "logout" %>
        </li>

        <li class="nav-item">
          <%= button_to "About", about_path, method: :get, class: "nav-link active", id: "logout" %>
        </li>

        <% if logged_in? %>

          <li>
            <%= button_to "Logout", logout_path, method: :delete, class: "nav-link active", id: "logout" %>
          </li>

        <% else %>

          <li class="nav-item">
            <%= button_to "Sign Up", signup_path, method: :get, class: "nav-link active", id: "logout" %>
          </li>

          <li class="nav-item">
            <%= button_to "Login", login_path, method: :get, class: "nav-link active", id: "logout" %>
          </li>

        <% end %>


      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>